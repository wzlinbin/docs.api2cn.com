客户端配置模板
下面这些模板可以直接复制。用户只需要把 放置你创建的api kye即可 换成自己在本站创建的 API Key，默认模型建议使用 gpt-5.5。

通用字段	应该填写
Base URL / API Host / OpenAI Compatible URL	https://api.api2cn.com/v1
API Key / Token	放置你创建的api kye即可
Model	gpt-5.5、gpt-5.4 或 gpt-image-2
接口协议	文本优先 Responses；不支持时用 Chat Completions。图片使用 /v1/images/generations。
1. Codex Desktop / Codex CLI
配置文件一般是 ~/.codex/config.toml 。Windows 用户通常在 %USERPROFILE%\.codex\config.toml 。只复制下面核心配置即可，不要复制别人电脑里的本地路径。

复制model = "gpt-5.5"
model_provider = "Api2CN"
model_reasoning_effort = "xhigh"
disable_response_storage = true

[model_providers.Api2CN]
name = "Api2CN"
base_url = "https://api.api2cn.com/v1"
wire_api = "responses"
experimental_bearer_token = "放置你创建的api kye即可"
如果你的 Codex 版本不识别 experimental_bearer_token ，可以改用环境变量方式：在 provider 里写 env_key = "Api2CN_API_KEY" ，然后把系统环境变量 Api2CN_API_KEY 设置为你的 API Key。
2. Claude Code
Claude Code 使用环境变量接入网关。推荐写到 ~/.claude/settings.json 的 env 里，或者在终端临时设置。

复制{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "放置你创建的api kye即可",
    "ANTHROPIC_BASE_URL": "https://api.api2cn.com/v1",
    "ANTHROPIC_MODEL": "gpt-5.5",
    "ANTHROPIC_CUSTOM_MODEL_OPTION": "gpt-5.5",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1"
  }
}
Claude Code 是 Anthropic 协议客户端。若某个版本提示模型或接口不兼容，优先改用 Codex、OpenCode、Continue 这类 OpenAI-compatible 客户端。
3. Cursor
Cursor 官方主要通过界面填写，不建议用户手动改内部配置文件。

Cursor 设置项	填写内容
位置	Cursor Settings → Models
OpenAI API Key	放置你创建的api kye即可
OpenAI Base URL / Override OpenAI Base URL	https://api.api2cn.com/v1
Model	gpt-5.5
如果你的 Cursor 版本没有 Base URL / Override Base URL 入口，就不能稳定直连本网关；这种情况下建议使用 Continue、Cline 或 OpenCode。
4. Continue
配置文件一般是 ~/.continue/config.yaml 。VS Code / JetBrains 插件都可以按这个思路配置。

复制name: Api2CN
version: 0.0.1
schema: v1

models:
  - name: Api2CN GPT-5.5
    provider: openai
    model: gpt-5.5
    apiBase: https://api.api2cn.com/v1
    apiKey: 放置你创建的api kye即可
    defaultCompletionOptions:
      temperature: 0
5. Cline / Roo Code
Cline 和 Roo Code 都适合用 OpenAI Compatible 模式。一般在插件设置里新增 Provider。

设置项	填写内容
API Provider	OpenAI Compatible
Base URL	https://api.api2cn.com/v1
API Key	放置你创建的api kye即可
Model ID	gpt-5.5
复制cline auth -p openai -k "放置你创建的api kye即可" -b https://api.api2cn.com/v1
6. OpenCode
在项目根目录或用户配置目录创建 opencode.json 。如果已经有配置，只合并 provider.Api2CN 和 model 相关字段。

复制{
  "$schema": "https://opencode.ai/config.json",
  "model": "Api2CN/gpt-5.5",
  "provider": {
    "Api2CN": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Api2CN",
      "options": {
        "baseURL": "https://api.api2cn.com/v1",
        "apiKey": "放置你创建的api kye即可"
      },
      "models": {
        "gpt-5.5": { "name": "GPT-5.5" },
        "gpt-5.4": { "name": "GPT-5.4" }
      }
    }
  }
}
7. OpenClaw
OpenClaw 可以在 openclaw.json 里添加自定义模型 Provider。下面示例使用 OpenAI-compatible 的 Chat Completions 适配器，适合先跑通文本模型。

复制{
  "models": {
    "mode": "merge",
    "providers": {
      "Api2CN": {
        "baseUrl": "https://api.api2cn.com/v1",
        "apiKey": "放置你创建的api kye即可",
        "api": "openai-completions",
        "models": [
          {
            "id": "gpt-5.5",
            "name": "Api2CN GPT-5.5",
            "reasoning": true,
            "input": ["text"],
            "output": ["text"],
            "contextWindow": 1050000,
            "maxTokens": 128000
          },
          {
            "id": "gpt-5.4",
            "name": "Api2CN GPT-5.4",
            "reasoning": true,
            "input": ["text"],
            "output": ["text"],
            "contextWindow": 1050000,
            "maxTokens": 128000
          }
        ]
      }
    }
  }
}
如果你的 OpenClaw 版本已稳定支持 Responses 适配器，也可以把 api 从 openai-completions 改为 openai-responses 测试。
8. CC-Switch 使用方法
CC-Switch 适合用来统一管理 Claude Code、Codex CLI、Gemini CLI、OpenCode、OpenClaw 等工具的 API Provider。添加一次智惠 API Provider 后，就可以在 CC-Switch 里一键切换到本站接口。

CC-Switch 配置项	填写内容
Provider Name	zhihui 或 Api2CN
API Type / Format	OpenAI Compatible
Base URL	https://api.api2cn.com/v1
API Key	放置你创建的api kye即可
Model	gpt-5.5 或 gpt-5.4
打开 CC-Switch，点击新增 Provider。
按上表填写 Provider 信息并保存。
在 CC-Switch 中选择刚创建的 Provider，并应用到需要使用的工具，例如 Claude Code、Codex CLI、OpenCode 或 OpenClaw。
回到对应终端工具，新开一个会话测试是否已经切换成功。
如果你的 CC-Switch 版本支持统一供应商（Universal Provider），建议优先创建统一供应商，这样 Claude Code、Codex CLI、Gemini CLI 等工具可以共用同一套智惠 API 配置。
快速开始
按下面三步即可完成第一次调用：

登录网站，在 密钥 / API Keys / Tokens 页面创建自己的 API Key。
在你的程序或客户端里填写 Base URL：https://api.api2cn.com/v1 。
请求时带上请求头：Authorization: Bearer YOUR_API_KEY 。
注意：Base URL 必须带 /v1 。不要把自己的 API Key 发给别人，也不要发到公开群、截图或代码仓库里。
复制curl https://api.api2cn.com/v1/responses \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{ "model": "gpt-5.5", "input": "只回复 ok", "store": false }'
模型与计费
当前面向用户建议只使用下面三个模型，其他模型如果没有展示或不可用，就不要填。

模型	适合场景	计费说明
gpt-5.5	更强文本、复杂推理、代码任务。	按站内价格扣费，通常约为 gpt-5.4 的 2 倍消耗。
gpt-5.4	标准文本模型，适合需要更低消耗的常规任务。	标准文本模型计费。
gpt-image-2	图片生成。	按图片生成价格扣费，不能当普通聊天模型使用。
余额单位以站内显示为准；扣费会区分输入、输出、缓存读取、图片生成等类型。实际余额和订单请以网站后台为准。
查看模型列表
如果你的客户端支持自动拉取模型，可以调用 /v1/models 。

复制curl https://api.api2cn.com/v1/models \
  -H "Authorization: Bearer YOUR_API_KEY"
Responses API
推荐优先使用 Responses API，适合新版 OpenAI SDK、推理参数和更统一的输入格式。

复制curl https://api.api2cn.com/v1/responses \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-5.5",
    "instructions": "You are a helpful assistant.",
    "input": "你好，介绍一下你自己。",
    "reasoning": {"effort": "medium"},
    "stream": false,
    "store": false
  }'
如果遇到 Unsupported parameter: temperature ，先移除 temperature 后重试。推理模型通常不需要手动设置温度。
Chat Completions
如果你的工具只支持老格式，就使用 /v1/chat/completions 。

复制curl https://api.api2cn.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-5.5",
    "messages": [
      {"role": "system", "content": "You are a helpful assistant."},
      {"role": "user", "content": "你好"}
    ],
    "stream": false
  }'
流式输出
把 stream 设置为 true ，模型会边生成边返回，适合聊天窗口。

复制curl https://api.api2cn.com/v1/responses \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-5.5",
    "input": "写一段 100 字介绍。",
    "stream": true,
    "store": false
  }'
图片生成
gpt-image-2 需要走图片生成接口，不要发到普通聊天接口。

复制curl https://api.api2cn.com/v1/images/generations \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-image-2",
    "prompt": "一只橘猫坐在未来城市窗边，电影感光线",
    "size": "1024x1024"
  }'
如果客户端没有图片接口，请在客户端里选择 OpenAI 图片生成接口，或直接用上面的 HTTP 请求测试。
Python / Node SDK
Python
复制from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_KEY",
    base_url="https://api.api2cn.com/v1",
)

response = client.responses.create(
    model="gpt-5.5",
    input="只回复 ok",
    store=False,
)

print(response.output_text)
Node.js
复制import OpenAI from "openai";

const client = new OpenAI({
    apiKey: process.env.API_KEY,
    baseURL: "https://api.api2cn.com/v1",
});

const response = await client.responses.create({
    model: "gpt-5.5",
    input: "只回复 ok",
    store: false,
});

console.log(response.output_text);
常见错误
401 / invalid key
API Key 填错、复制少了字符，或密钥已被删除。

余额不足
账户没有可用余额，需要先充值或联系管理员处理。

429 / limit
上游账号达到并发或额度限制，稍后重试。

502 Bad Gateway
上游网络或账号临时异常，一般重试即可；频繁出现请联系管理员。

503 Unavailable
当前模型没有可用渠道，或模型名填错。

Unsupported parameter
移除报错里提到的参数，例如 temperature 或 max_output_tokens 。

上线检查清单
Base URL 是否填写为 https://api.api2cn.com/v1 。
请求头是否包含 Authorization: Bearer YOUR_API_KEY 。
文本模型是否使用 gpt-5.5 或 gpt-5.4 。
图片模型是否使用 /v1/images/generations 和 gpt-image-2 。
不要在代码仓库、截图、群聊里公开 API Key。
推荐：先用 gpt-5.5 测通
注意：gpt-image-2 不是聊天接口
兼容：OpenAI SDK
优秀插件介绍
claude-code-skills 是一组面向 Claude Code 的开源 skills。这里重点推荐 codex 和 codex-plan：前者适合让 Codex CLI 给代码、方案或问题提供第二意见；后者适合在动手开发前做多轮对抗式计划审查。

推荐安装前准备
先安装 Codex CLI，并执行 codex login 完成登录。
确认 ~/.codex/config.toml 已配置好可用模型和 Provider。
Claude Code 的 skills 目录一般是 ~/.claude/skills/，Windows 可对应到 %USERPROFILE%\.claude\skills\。
复制npm install -g @openai/codex
codex login
安装codex-plan skills
复制直接输入：请阅读 https://github.com/lldxflwb/claude-code-skills 帮我安装里面的 codex 和 codex-plan skill
复制git clone https://github.com/lldxflwb/claude-code-skills.git
mkdir -p ~/.claude/skills
ln -s /path/to/claude-code-skills/skills/codex ~/.claude/skills/codex
ln -s /path/to/claude-code-skills/skills/codex-plan ~/.claude/skills/codex-plan
如果不想使用软链，也可以直接把仓库里的 skills/codex 和 skills/codex-plan 两个文件夹复制到 ~/.claude/skills/。
Skill	适合场景	典型用法
codex	让 Codex CLI 从 GPT 模型视角复核代码、分析问题、审查变更或给方案第二意见。	/codex review 一下当前代码变更
/codex 分析这个架构方案的风险
codex-plan	让 Claude 和 Codex 围绕计划多轮讨论：Claude 写计划，Codex 挑问题，Claude 评估并修改，直到计划足够稳。	/codex-plan @docs/migration-plan.md
/codex-plan 重构用户认证模块
使用建议
小问题、代码复核、方案第二意见，用 /codex。
重要重构、迁移方案、架构设计，先用 /codex-plan 过一遍。
codex 默认让 Codex 以只读方式参与，不会直接改文件；最终是否采纳由你决定。
codex-plan 更适合在动手前使用，可以提前暴露边界情况、风险点和过度设计。
如果你已经按上面的 CC-Switch 方法配置好智惠 API，可以先确保 Codex CLI 使用本站 Provider，再安装这两个 skills，这样 Claude Code 调用 Codex 复核时也会走同一套模型配置。
最后更新：2026-05-05，已新增 Codex、Claude Code、Cursor、Continue、Cline/Roo Code、OpenCode、OpenClaw 配置模板、CC-Switch 使用方法和优秀插件介绍。本文档只展示接入方式，模型可用性和余额以网站后台实时状态为准。